import styled from 'styled-components';

const LoginModal = () => {
    return (
        <LoginModalWrapper>

        </LoginModalWrapper>
    );
}

const LoginModalWrapper = styled.div``

export default LoginModal;
